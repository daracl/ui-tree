// checkbox state
export const CHECK_STATE = {
  CHECKED: 1,
  UNCHECKED: 2,
  INDETERMINATE: 3,
} as const;

//move position
export const MOVE_POSITION = {
  PREV: "prev",
  CHILD: "child",
  NEXT: "next",
} as const;
